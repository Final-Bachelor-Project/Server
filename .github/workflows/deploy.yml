name: deployment
on:
  push:
    branches: [ master]
  workflow_dispatch:
jobs:
  deployment:
    runs-on: self-hosted
    
    steps:
      - name: Restart server application
        run: pm2 restart server

