name: deployment
on:
  push:
    branches: [ master]
  workflow_dispatch:
jobs:
  buildAndTest:
    needs: ./.github/workflows/buildAndTest.yml

  deployment:
    runs-on: self-hosted
    steps:
      - name: Restard server
        run: pm2 restart tuneBudyServer

